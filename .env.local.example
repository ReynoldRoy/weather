# Firebase Project Credentials
# IMPORTANT: Replace the placeholder values below with your actual Firebase project credentials.
# You can find these in your Firebase project settings:
# Firebase Console -> Project settings (gear icon) -> General tab -> Your apps -> SDK setup and configuration (select Config).
#
# DO NOT commit your actual credentials in .env.local to version control if this is a public repository.
# .env.local is typically included in .gitignore for this reason.

# Required: Your Firebase Web API Key
NEXT_PUBLIC_FIREBASE_API_KEY="PLACEHOLDER_API_KEY_REPLACE_ME"

# Required: Your Firebase Auth Domain (e.g., your-project-id.firebaseapp.com)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="PLACEHOLDER_AUTH_DOMAIN_REPLACE_ME_your-project-id.firebaseapp.com"

# Required: Your Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID="PLACEHOLDER_PROJECT_ID_REPLACE_ME"

# Optional but recommended: Your Firebase Storage Bucket (e.g., your-project-id.appspot.com)
# Required if you use Firebase Storage.
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="PLACEHOLDER_STORAGE_BUCKET_REPLACE_ME_your-project-id.appspot.com"

# Optional but recommended: Your Firebase Messaging Sender ID
# Required if you use Firebase Cloud Messaging.
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="PLACEHOLDER_MESSAGING_SENDER_ID_REPLACE_ME_123456789012"

# Optional but recommended: Your Firebase App ID
# Required for some Firebase services like Google Analytics.
NEXT_PUBLIC_FIREBASE_APP_ID="PLACEHOLDER_APP_ID_REPLACE_ME_1:123456789012:web:abc"

# Optional: Your Firebase Measurement ID (for Google Analytics)
# Required if you use Google Analytics for Firebase.
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="PLACEHOLDER_MEASUREMENT_ID_REPLACE_ME_G-XYZ"

# Note: After creating/modifying .env.local, you MUST restart your Next.js development server
# for the changes to take effect.
