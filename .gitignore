# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Testing
/coverage
*.lcov

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local Environment Variables
# Exclude all .env files apart from .env.example and .env.local.example
.env*
!.env.example
!.env.*.example
# If you want to allow .env.local for specific non-sensitive overrides (generally not recommended for team projects)
# you might adjust, but the common practice is to ignore all .env*.local files.
# For this project, we will ensure .env.local is ignored.
.env.local
.env.development.local
.env.production.local
.env.test.local


# TypeScript
*.tsbuildinfo
next-env.d.ts

# Firebase
firebase-debug.log
firebase-debug.*.log
# If you use Firebase emulators and generate export data
# .firebase/emulators/
# firebase-export-*

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace
*.sublime-project

# JetBrains IDEs folder
.idea

# Output of 'npm pack'
*.tgz

# Mac files
.AppleDouble

# Windows thumbnail cache files
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# Genkit (if any local state files are generated and should not be committed)
# .genkit/
# .genkit_state.json
# genkit-debug.log
# genkit-debug.*.log
