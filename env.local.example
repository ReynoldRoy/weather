# Firebase Configuration - IMPORTANT: Replace with your actual Firebase project credentials.
# Obtain these from your Firebase project console (Project settings > General).
# Create a .env.local file in the root of your project and copy these lines into it,
# then fill in the values. DO NOT commit your .env.local file to version control.

NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_FIREBASE_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_FIREBASE_AUTH_DOMAIN"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_FIREBASE_PROJECT_ID"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_FIREBASE_STORAGE_BUCKET" # Optional, if using Firebase Storage
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_FIREBASE_MESSAGING_SENDER_ID" # Optional, if using Firebase Cloud Messaging
NEXT_PUBLIC_FIREBASE_APP_ID="YOUR_FIREBASE_APP_ID" # Optional for some setups, but generally good to include
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="YOUR_FIREBASE_MEASUREMENT_ID" # Optional, if using Google Analytics for Firebase

# WeatherAPI.com Configuration
# The WeatherAPI key is currently hardcoded in src/lib/config.ts.
# For better practice, especially if sharing your project, consider moving it here:
# NEXT_PUBLIC_WEATHERAPI_COM_API_KEY="YOUR_WEATHERAPI_COM_API_KEY"
# If you do, update src/lib/config.ts to read from process.env.
